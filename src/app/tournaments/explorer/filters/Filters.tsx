import { FC } from 'react'

import ChangeType from './ChangeType'

const Filters: FC = ({}) => {
	return (
		<div>
			<ChangeType />
			{/* <CategoryGroup /> */}
		</div>
	)
}

export default Filters
